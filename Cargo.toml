[package]
name = "heston-mc"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = { version = "0.8", features = ["small_rng"] }
rand_distr = "0.4"
rayon = "1"
plotters = "0.3"

[profile.release]
opt-level = 3
lto = "thin"

[profile.release.build-override]
opt-level = 3
